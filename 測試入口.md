# 🧪 測試入口

改完東西不知道該跑什麼的時候，看這一頁就好。全部指令都在專案根目錄執行。

---

## 一、三秒版

```bash
python3 scripts/verify_problems.py   # 27 秒
node scripts/verify_courses.mjs      #  4 秒
node scripts/verify_editors.cjs      #  0 秒
node --test "test/*.test.mjs" "algoplay/test/*.test.mjs"   # 0 秒
```

四個都跑完不到 35 秒。**不確定該跑哪個就四個都跑。**

> 四個現在都應該全綠、離開碼 0：`verify_editors.cjs` 是 `13 passed; 0 failed`，最後一個是 `48 pass / 0 fail`。**看到任何一個紅字就是你剛改壞的**，不要跳過。

---

## 二、我改了什麼 → 該跑什麼

| 我動的東西 | 必跑 | 還要開瀏覽器嗎 |
| --- | --- | --- |
| `problems/*.json`、`solutions/*.py` | `verify_problems.py` | 不用 |
| `beginner.html` 的講義資料 | `verify_courses.mjs` | 換版時要 |
| `assets/course-catalog.js`（LV.2） | `verify_courses.mjs` | 換版時要 |
| `assets/advanced-course.js`（LV.3） | `verify_courses.mjs` | 換版時要 |
| 關卡數量增減 | `verify_courses.mjs` | **要**，且見第五節 |
| `assets/blockly-editor.js`、`scratch-editor.js` | `verify_editors.cjs` | **要**，跑 Scratch 頁 |
| `assets/programming-editor.js`、判題流程 | `verify_editors.cjs` | **要**，跑整頁測試 |
| `judge.html`、`index.html`、`guide.html` | — | **要**，手動看一遍 |
| `algoplay/` 的小鎮、關卡、模組 | `node --test "algoplay/test/*.test.mjs"` | **要**，畫面要自己看 |
| `test/` 的流程圖實驗室 | `node --test "test/*.test.mjs"` | **要**，畫面要自己看 |
| 只改 `.md` 文件 | 不用跑 | 不用 |

---

## 三、四個終端指令在測什麼

| 指令 | 測什麼 | 跑不過代表 |
| --- | --- | --- |
| `python3 scripts/verify_problems.py` | 214 題 Judge 題庫的 JSON 結構、`stage`／`apcsLevel` 合法值、每題 Python 參考解答跑過全部測資 | 題目格式錯，或參考解答輸出跟測資對不上 |
| `node scripts/verify_courses.mjs` | 三階段講義的關數、必填欄位、每關剛好 2 個變體、每題至少 3 組測資，**並且實際用 python3 跑每一題的 `solution` 比對 `tests`** | 關數對不上 `expectedCounts`，或某題參考解答是錯的 |
| `node scripts/verify_editors.cjs` | 語言切換、草稿相容性、重設、過期訊息、Blockly 產生的 Python 實際輸入輸出，外加 `index.json` 與每個題目 JSON 的五個分級欄位對拍 | 編輯器狀態管理壞了，或索引與題目檔不同步 |
| `node --test "test/*.test.mjs" "algoplay/test/*.test.mjs"` | 流程圖實驗室的核心引擎與連線繞路，演算法小鎮的地圖、關卡邏輯與機器人程式 | 流程圖執行／演算法遊樂場的邏輯壞了 |

`verify_courses.mjs` 會真的開 324 個 python3 行程跑測資，**它說通過就是真的通過**，不是只檢查欄位有沒有填。

---

## 四、瀏覽器測試（兩個頁，兩個不同連接埠）

網站用 iframe 與 Web Worker，**不能用 `file://` 直接開**，一定要起 HTTP 伺服器。

### 4.1 Scratch 整合驗證 — 連接埠 8080

```bash
python3 -m http.server 8080
```

開 <http://localhost:8080/scripts/verify-scratch.html>，頁面會自動跑。測真實的 Scratch Blocks、Scratch VM 與 Worker，補終端測不到的部分。

### 4.2 完整作答流程整頁測試 — 連接埠 8081

```bash
python3 -m http.server 8081 --bind 127.0.0.1
```

開 <http://127.0.0.1:8081/scripts/verify-integration.html>，按 **Run tests**。

- **一定要用 8081**，這是測試專用來源，才不會污染你平常 8080 的草稿與進度。
- 第一次會下載 Pyodide，可能要好幾分鐘。
- **測試途中不要關頁面**，它結束後才會還原觸及的草稿。

### 4.3 手動看一遍（自動測涵蓋不到）

四個頁面各確認一次：

```
beginner.html
beginner.html?course=intermediate
beginner.html?course=advanced
judge.html
```

每頁確認：允許的作答方式能編輯也能判題、切題與重新整理能還原草稿、重設不影響其他語言、積木匯出後能重新匯入。**執行途中不要切題或重設。**

Blockly 範例作品：`scripts/fixtures/blockly-sum-range.json`、`blockly-sum-list.json`。

---

## 五、⚠️ 增減關卡時最容易忘的連動清單

改關卡數量光跑驗證不夠，下面這些沒改會讓文件失真或讓學生進度錯亂。

### 一定要改

| 位置 | 為什麼 |
| --- | --- |
| `scripts/verify_courses.mjs` 的 `expectedCounts` | 不改的話驗證直接紅字 |
| `README.md`（關數題數出現在 5 個地方） | 開頭簡介、學習路徑段、功能特色清單、驗證段落、現況限制表；用 `grep -n "15 關\|35 關\|105 題" README.md` 找 |
| `docs/題庫總覽與學習階梯.md` | 開頭的「本站講義合計 N 關、M 題」 |
| `docs/課程內容草稿/課程與題庫總綱.md` | 第一節概念流程、第三節銜接節點表、第八節三語言支援邊界 |
| `for_AI/小學程式基礎學習地圖_第一階段.md` | LV.1 的教學設計母稿：依賴地圖、概念教學卡、排課表、分支示意圖 |

### 如果動到 id 編號，還要改

| 位置 | 為什麼 |
| --- | --- |
| `BEGINNER_COURSE.progressVersion` | 不升版的話舊進度會對到錯的關卡 |
| `BEGINNER_COURSE.legacyDrafts` | 設 `true` 時會去撈 pre-v2 的單鍵草稿，重編號後會把舊關卡的草稿還原到新關卡 |
| `migrateLegacyProgress()` 的 `skipLegacyProgress` 保護 | 新版本鍵不存在時它會讀舊的 `pyjudge_beginner_completed`（存舊 id）回填，會誤標完成 |
| `finishTitle` / `finishCopy` | 結業文案會列出學過的概念 |

進度鍵長這樣，換版前先確認自己知道會影響誰：

```
pyjudge_<階段>_completed
pyjudge_<階段>_task_progress_<版本>
pyjudge_<階段>_draft_<關卡 id>_<core/a/b>
```

### 不用改

`index.html` 與 `guide.html` 沒有寫死關數，不用跟。

---

## 六、送出前最後一次

```bash
python3 scripts/verify_problems.py && node scripts/verify_courses.mjs && node scripts/verify_editors.cjs && node --test "test/*.test.mjs" "algoplay/test/*.test.mjs"
```

四個正常時離開碼都是 0，所以可以放心用 `&&` 串：**中途斷掉就是真的有東西壞了**。

再加上：`git diff --stat` 掃一眼有沒有改到不該改的檔案。
