# 講義母稿補完實施計畫

本文件累積各輪補完計畫。最新一輪在最下方。

| 輪次 | 主題 | 狀態 |
| --- | --- | --- |
| 第一輪（2026-09-22） | LV.1 補 `while`、LV.2／LV.3 補先預測與概念短講 | ✅ 已完成並上線 |
| 第二輪（2026-09-25） | 學習地圖缺口補完：LV.1 +4 關、LV.2 +4 關、LV.3 +6 關 | 🚧 LV.1 ✅、LV.2 ✅、LV.3 待實作 |

---

# 第一輪（已完成）

## 修改範圍總覽

| 檔案                                                                                                                                              | 動作                           | 規模       |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------- |
| [小學程式基礎學習地圖_第一階段.md](for_AI/小學程式基礎學習地圖_第一階段.md) | 新增`while` 關卡（概念 8.5） | +1 關      |
| [LV.2講義內容母稿.md](docs/課程內容草稿/LV.2講義內容母稿.md)                | 補齊缺漏觀念                   | 修改 10 關 |
| [LV.3講義內容母稿.md](docs/課程內容草稿/LV.3講義內容母稿.md)                | 擴充 + 標記 Python 專屬        | 修改 10 關 |
| [課程與題庫總綱.md](docs/課程內容草稿/課程與題庫總綱.md)                    | 同步更新階梯表                 | 更新表格   |

---

## 一、LV.1：新增 `while` 迴圈

### 位置

插入於現有 L8（for 迴圈）與 L9（計數器）之間，符合學習地圖的路線：

```
L8 for 迴圈 → 【新增】while 迴圈 → L9 計數器 → L10 累加器
```

> [!NOTE]
> 這會使LV.1從 10 個概念變為 11 個。beginner.html 的修改留到後續網頁整合階段；現階段先在學習地圖文件中補上母稿內容。

### 內容方向

| 元素                   | 內容                                                                     |
| ---------------------- | ------------------------------------------------------------------------ |
| **概念名稱**     | 重複直到 — while 迴圈                                                   |
| **Badge**        | `while`                                                                |
| **Scratch 對應** | 「重複直到」積木                                                         |
| **Blockly 對應** | `controls_whileUntil`（重複當 / 重複直到）                             |
| **教學目標**     | 理解「不知道要重複幾次，直到條件不成立才停止」與 for 的差異              |
| **核心對比**     | `for`：你已經知道要重複幾次 → `while`：你不知道，但知道什麼時候要停 |
| **教師例題**     | 存錢計畫：每天存 N 元，存到達標為止，輸出需要幾天                        |
| **變體 A**       | 倒數到零：輸入正整數，每次減 3，輸出減到 0 以下前最後的值                |
| **變體 B**       | 猜數字追蹤：固定數字逐步對半搜尋，輸出幾步找到（銜接未來二分搜尋）       |
| **常見錯誤**     | 忘記更新條件變數 → 無窮迴圈；搞混「重複直到」和「重複當」的條件方向     |

---

## 二、LV.2：補齊缺漏觀念

### 2.1 每關補齊「先預測」（M03–M10，共 8 關）

為每關寫一個 3 分鐘手動追蹤題，格式同LV.1的 `predict / predictAnswer`。

| 關卡 | 先預測方向                                                           |
| ---- | -------------------------------------------------------------------- |
| M03  | `"3 8 6".split()` 的結果是什麼？每一項的型別是？                   |
| M04  | 手算 5 筆資料的平均（故意不能整除），整數除法結果是？                |
| M05  | 全為負數`[-5, -2, -8]`，如果 best 初始為 0 會怎樣？                |
| M06  | 清單`[4, 7, 2, 7]` 中搜尋 7：找到/次數/位置三種問法各答什麼？      |
| M07  | 三筆指令`(2, +3)(2, -1)(2, +5)` 後，第 2 格從初始 0 變成？         |
| M08  | R=2, C=3 時外層跑幾次？內層總共跑幾次？                              |
| M09  | `grid[1][2]` 在紙上的位置（第 2 列第 3 欄）                        |
| M10  | `clamp(120, 0, 100)` 的回傳值？如果函式只 print 不 return 會怎樣？ |

### 2.2 補齊「概念短講」（M06、M08、M09）

| 關卡 | 補充內容                                                                                                  |
| ---- | --------------------------------------------------------------------------------------------------------- |
| M06  | 搜尋有三種問法（存在、次數、位置），解法不同。布林變數`found = False` 的用途。                          |
| M08  | 用教室座位比喻：外層控制「第幾排」，內層控制「這排的第幾個」。每次外層走一步，內層要跑完整一輪。          |
| M09  | 二維清單是「清單的清單」。`grid[r]` 取出一整列，`grid[r][c]` 才取到單一格子。用棋盤格紙畫出座標對應。 |

### 2.3 字串切片（加入 M01 概念短講）

在 M01 現有的「索引」教學之後，補充字串切片的最小必要知識：

```python
text = "PYTHON"
text[1:4]    # "YTH" — 從 1 到 3（不含 4）
text[:3]     # "PYT" — 從頭到 2
text[2:]     # "THON" — 從 2 到最後
text[::-1]   # "NOHTYP" — 反轉
```

### 2.4 型別轉換（加入 M03 概念短講）

在 M03 教 `split()` 和 `map(int, ...)` 時，正式解釋型別轉換：

- `input()` 永遠回傳字串 → `"3"` 不是數字 `3`
- `"3" + "5"` 得到 `"35"`（字串拼接），`int("3") + int("5")` 得到 `8`
- `split()` 把一行字串切成多個字串
- `map(int, ...)` 把每一段字串都轉成整數

### 2.5 布林值與 `and/or/not`（加入 M06 概念短講）

在 M06（線性搜尋）教 `found = False` 時，順帶正式介紹布林值：

- `True` / `False` 是 Python 的兩個布林值
- 比較運算 `x > 5` 的結果就是布林值
- `and`：兩邊都成立才是 True
- `or`：任一邊成立就是 True
- `not`：反轉
- M06 的 `found` 就是一個布林變數

### 2.6 字串方法速查（加入 M02 概念短講尾端）

在 M02 教完 `isdigit()` 後，附一張常用方法速查表：

| 方法                          | 用途                | 範例                                    |
| ----------------------------- | ------------------- | --------------------------------------- |
| `s.upper()` / `s.lower()` | 轉大/小寫           | `"Hello".lower()` → `"hello"`      |
| `s.strip()`                 | 去除首尾空白        | `" hi ".strip()` → `"hi"`          |
| `s.replace(a, b)`           | 取代                | `"cat".replace("c","b")` → `"bat"` |
| `s.find(x)`                 | 找位置（-1=找不到） | `"hello".find("ll")` → `2`         |
| `s.count(x)`                | 計數                | `"banana".count("a")` → `3`        |
| `x in s`                    | 是否包含            | `"a" in "cat"` → `True`            |

### 2.7 除錯提示（加入 M03 常見錯誤段落）

從 M03 開始，在「常見錯誤」段落中標注對應的 Python 錯誤類型：

```
常見錯誤：
- 忘了轉 int → TypeError: '>' not supported between instances of 'str' and 'int'
- K 沒有減 1 → IndexError: list index out of range
- 假設清單只有一位數 → ValueError: invalid literal for int()
```

---

## 三、LV.3：擴充 dict/set 教學 + 標記 Python 專屬

### 3.1 大幅擴充 H01 概念短講

H01 目前只有一句帶過 set/dict。擴充為完整教學，分三段：

**第一段：效率觀念**（保留原有內容）

- N = 200,000 時 O(N²) 不可行，O(N) 可行

**第二段：集合 set 教學** `🐍 Python 專屬`

```python
seen = set()          # 建立空集合
seen.add(5)           # 加入元素
seen.add(3)
5 in seen             # True — O(1) 查找
seen.add(5)           # 不會重複
len(seen)             # 2
```

**第三段：字典 dict 教學** `🐍 Python 專屬`

```python
count = {}            # 建立空字典
count["apple"] = 3    # 新增鍵值對
count["apple"] += 1   # 更新值
"apple" in count      # True — O(1) 查找
for key in count:     # 走訪所有鍵
    print(key, count[key])
```

### 3.2 H02 補充 tuple 基礎

在 H02 概念短講中加入：

```python
# tuple 是不可變的序列，常用於排序鍵
pair = (90, "Amy")    # 用小括號或直接逗號
pair[0]               # 90
# 比較規則：先比第一項，相同再比第二項
(-90, "Amy") < (-90, "Bob")  # True（分數相同，Amy 字母較小）
```

### 3.3 補齊先預測（H02–H10）

| 關卡 | 先預測方向                                                            |
| ---- | --------------------------------------------------------------------- |
| H02  | 把`[(90,"Amy"),(85,"Bob"),(90,"Ann")]` 依分數高到低排，同分怎麼辦？ |
| H03  | 資料`[2,4,1,3,5]`，手算 prefix 陣列；查詢 2–4 的總和是？           |
| H04  | 在`[1,4,7,10,13]` 中找 7，最多需要幾次比較？                        |
| H05  | 字串`"([)]"` 的括號配對追蹤，堆疊何時會發現錯誤？                   |
| H06  | 3×3 地圖中 S 到 T 的 BFS 展開，第幾步到達？                          |
| H07  | `digit_sum(123)` 的遞迴展開：123 → 3 + digit_sum(12) → ...        |
| H08  | 活動`[1,3) [2,5) [3,4)`，選最早結束的策略為什麼能最多？             |
| H09  | 爬樓費用`[10,15,20]`，畫出 dp[0], dp[1], dp[2] 的推導過程           |
| H10  | 3×3 地圖中有幾個連通區域？最大的有幾格？                             |

### 3.4 補齊概念短講（H05、H07、H10）

| 關卡 | 補充內容                                                                                                                                                                |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H05  | 堆疊是一疊盤子：最後放上去的最先拿起來（後進先出）。Python 用 list 的`append()` 和 `pop()` 就是堆疊。                                                               |
| H07  | 遞迴的三個要素：①什麼時候停（基底案例）②怎麼把問題變小③怎麼用小問題的答案組合大問題的答案。Python 預設遞迴深度 1000，超大問題要改用迴圈或`sys.setrecursionlimit`。 |
| H10  | 這一關不教新工具，而是練習「建模」：看到題目後，先回答四個問題 — 節點是什麼？邊是什麼？需要的資訊是什麼？適合用 BFS 還是 DFS？                                         |

### 3.5 標記 Python 專屬題目

在以下題目的描述中加入 `🐍 Python 專屬` 標記，並說明原因：

| 關卡/題目                      | 原因                                                             |
| ------------------------------ | ---------------------------------------------------------------- |
| H01 核心題 C（是否有重複編號） | 需要`set`                                                      |
| H01 變體 A（不同編號數量）     | 需要`set`                                                      |
| H01 變體 B（最常出現的編號）   | 需要`dict`                                                     |
| Judge 007（兩數之和）          | 需要`dict` / hash table                                        |
| H02 全部                       | 需要`tuple` 作排序鍵（Blockly 有 `lists_sort` 但無法自訂鍵） |

---

## 四、同步更新課程與題庫總綱

- LV.1階梯表增加 `while` 行
- LV.2階梯表的先備欄位更新（M07 加入 while 先備）
- LV.3 H01 標記為「Python 專屬概念引入點」
- 增加一個「三語言支援邊界」段落，說明哪些概念只有 Python 才能完成

---

## 執行順序

1. ✏️ 更新LV.1學習地圖：新增 while 概念
2. ✏️ 更新LV.2母稿：補齊 8 關先預測 + 3 關概念短講 + 字串切片/型別/布林/方法/除錯
3. ✏️ 更新LV.3母稿：擴充 H01 + H02 + 補 9 關先預測 + 3 關概念短講 + Python 標記
4. ✏️ 更新課程與題庫總綱：同步階梯表與三語言邊界說明

## Verification Plan

- 逐關檢查每個教學元素（goal/predict/concept/starter/hints/tests/mistakes/checkpoint）是否齊全
- 確認先備知識鏈沒有斷裂（每關的「新能力」不超過一個）
- 確認 Python 專屬標記只出現在 dict/set/tuple 必需的題目

---
---

# 第二輪（2026-09-25）：學習地圖缺口補完

## 〇、盤點依據與結論

比對方式：抽出 `beginner.html` 的 `BEGINNER_LESSONS`、`assets/course-catalog.js`（M01–M10）、`assets/advanced-course.js`（H01–H10）每一關的 `goal` 與 `concept`，與 `problems/*.json` 共 216 檔的 `tags` 做交叉比對。

三個主要結論：

1. **題庫有 4 級，講義只有 3 級。** `README.md` 定義了 `Challenge` 級且題庫有 11 題（007、204–213），但沒有任何講義。本輪先不處理（見第四節）。
2. **題庫 43 題掛「數學」tag，講義完全沒有這條線。** 進位轉換、質數、因數、累乘、位數拆解在 LV.1／LV.2 都沒有落點。
3. **LV.1 的語法地基有洞。** `input()` 回傳字串、`//` 與 `%`、小數格式化、布林值本身，四件事都沒有獨立關卡，但後面的關卡與題庫大量依賴它們。

## 一、本輪總規模

| 階段 | 現況 | 本輪目標 | 增加 |
| --- | ---: | ---: | --- |
| LV.1 | 11 關 | **15 關** | +4（型別、整除餘數、小數格式化、布林值） |
| LV.2 | 10 關 | **14 關** | +4（清單增刪切片、字串組裝、數字拆解與數論、除錯） |
| LV.3 | 10 關 | **16 關** | +6（基礎排序、進階排序、雙指標滑動視窗、圖與樹、記憶化遞迴、進階 DP） |
| LV.4 | 0 關 | 本輪不做 | — |
| 合計 | 31 關 93 題 | **45 關 135 題** | +14 關 +42 題 |

實作順序：**LV.1 → 驗收 → LV.2 → 驗收 → LV.3**。本文件先把三階段規格寫完，實作分批進行。

---

## 二、LV.1：11 關 → 15 關

### 2.1 編號決策：全部重排成 1–15

不沿用 8.5 那種小數插入法。`id` 與 `displayNumber` 一律改成連續整數 1–15，含原本的 `'8-5'`。

**代價與處理**：`pyjudge_beginner_completed` 與 `pyjudge_beginner_draft_*` 存的是舊 id，重排後會對到錯的關卡。本輪採「不寫遷移、直接換版」：

| 項目 | 動作 |
| --- | --- |
| `BEGINNER_COURSE.progressVersion` | `'v2'` → `'v3'`，等同開一份新進度 |
| `BEGINNER_COURSE.legacyDrafts` | `true` → `false`。這個旗標讓 `beginner.html:2163` 去撈 pre-v2 的單鍵草稿 `pyjudge_beginner_draft_<id>`；重排後它會把舊關卡的草稿還原到新關卡，必須關掉 |
| `migrateLegacyProgress()`（`beginner.html:2036`） | 需加保護。v3 鍵不存在時它會讀 `pyjudge_beginner_completed`（舊 id）回填，會誤標新關卡完成。作法：在 `BEGINNER_COURSE` 加 `skipLegacyProgress: true`，函式開頭改為 `if (COURSE.skipLegacyProgress \|\| localStorage.getItem(STORAGE_PROGRESS)) return;` |

`storagePrefix` 維持 `pyjudge_beginner` 不動。

### 2.2 新的 15 關順序

| 新 | 關卡 | 來源 |
| ---: | --- | --- |
| 1 | 精確輸出 | 原 1 |
| 2 | 指令順序 | 原 2 |
| 3 | 輸入與回應 | 原 3 |
| 4 | 變數與更新 | 原 4 |
| **5** | **文字還是數字** | ★ 新增 |
| 6 | 算術運算 | 原 5（變體 B 要換掉，見 2.4） |
| **7** | **整除與餘數** | ★ 新增（吸收原 5 的變體 B） |
| **8** | **小數與格式化輸出** | ★ 新增 |
| 9 | 比較與二選一 | 原 6 |
| 10 | 多種情況 | 原 7 |
| **11** | **布林值與 and / or / not** | ★ 新增 |
| 12 | 指定次數重複 | 原 8 |
| 13 | 重複直到 | 原 `'8-5'` |
| 14 | 計數器 | 原 9 |
| 15 | 累加器 | 原 10 |

除了 6 的變體 B，原有 11 關的教學內容**一字不改**，只改 `id`。

> **實作修正（2026-09-25）**：原本規格要求為 15 關都補上 `displayNumber`，這是錯的。`lessonNumber()`（`beginner.html:2288`）的 fallback 是 `lesson.displayNumber || String(lesson.id).padStart(2, '0')`；id 全部變成整數之後，fallback 本身就會產生 `01`–`15`，維持原本的零補位外觀。補上數字型的 `displayNumber` 反而會讓第 1–9 關從 `01`–`09` 變成 `1`–`9`。最終作法是**全部不設 `displayNumber`**，只有 `id`。

### 2.3 四個新關卡規格

每關沿用既有欄位：`id / displayNumber / title / badge / kicker / goal / concept / mission / sampleInput / sampleOutput / predict / predictAnswer / steps / starter / solution / hint / checkpoint / commonMistakes / tests / variants`。核心題與兩個變體各至少 3 組 `tests`。

#### 第 5 關：文字還是數字

| 欄位 | 內容 |
| --- | --- |
| badge | `int()` · `str()` |
| kicker | `TWO KINDS OF DATA` |
| goal | 分辨文字與數字兩種資料，知道 `input()` 拿到的一定是文字，要做數學前必須先轉成數字。 |
| concept | `input()` 永遠回傳文字。文字用 `+` 是接在一起（`"3" + "5"` 得到 `"35"`），數字用 `+` 才是相加（`3 + 5` 得到 `8`）。`int()` 把文字轉成整數，`str()` 把數字轉回文字。Scratch 與 Blockly 會自動判斷該用哪一種，Python 要自己說清楚。 |
| predict | 兩行分別輸入 `3` 和 `5`，程式寫 `print(a + b)` 而且沒有轉換，會輸出什麼？ |
| predictAnswer | `35`。a、b 都是文字，`+` 做的是接在一起。要得到 `8` 必須寫 `int(a) + int(b)`。 |
| 核心題 | 輸入兩行整數。第一行輸出兩數接在一起的文字，第二行輸出兩數相加的結果。`3\n5` → `35\n8` |
| 變體 A | 編號補字：輸入整數 n，輸出 `No.` 接上 n。教 `str()`。 |
| 變體 B | 先轉再算：輸入一行兩個整數（空白分隔），輸出相加。用 `input().split()` 取得兩段**文字**，再各自 `int()`。銜接第 6 關的 `map(int, ...)`。 |
| commonMistakes | 以為 `input()` 給的是數字、直接拿去做數學 → `TypeError`；把 `int()` 套在整個 `input().split()` 上。 |

#### 第 7 關：整除與餘數

| 欄位 | 內容 |
| --- | --- |
| badge | `//` · `%` |
| kicker | `HOW MANY, HOW MANY LEFT` |
| goal | 用整除回答「可以分成幾份」，用餘數回答「還剩幾個」。 |
| concept | `7 // 2` 是 `3`，只留整數部分，**不是四捨五入**。`7 % 2` 是 `1`，是分完剩下的。想知道能不能整除，就看 `%` 是不是 0。 |
| predict | 23 顆糖每袋裝 5 顆。`23 // 5` 和 `23 % 5` 各是多少？ |
| 核心題 | 分組後剩幾個：輸入總數 N 與每組人數 K，第一行輸出完整幾組，第二行輸出剩幾人。（**直接沿用原第 5 關變體 B 的題意，升格為核心題**） |
| 變體 A | 秒數換算：輸入總秒數，輸出「幾分 幾秒」。 |
| 變體 B | 拆出個位與十位：輸入正整數，輸出個位數與十位數。銜接 LV.2 的數字拆解與進位轉換。 |
| commonMistakes | 用 `/` 得到小數（`23 / 5` 是 `4.6` 不是 `4`）；負數的 `//` 會往下取整，本關限定正整數避免混淆。 |

> 這一關必須排在「比較與二選一」之前，所以**不能使用 `if`**。三題都只做純算術與輸出。

#### 第 8 關：小數與格式化輸出

| 欄位 | 內容 |
| --- | --- |
| badge | `/` · `f"{x:.2f}"` |
| kicker | `SHOW THE RIGHT DIGITS` |
| goal | 分清楚 `/` 與 `//`，並把小數輸出成題目要求的位數。 |
| concept | `/` 一定得到小數，`8 / 2` 是 `4.0` 不是 `4`。要固定幾位小數，用 `f"{x:.2f}"`：小數第二位之後四捨五入，不足的位數會補 0。題目說「小數點後兩位」時，`3.5` 要輸出成 `3.50`。 |
| predict | `print(100 / 3)` 會印出什麼？題目只要小數點後兩位時要怎麼改？ |
| 核心題 | 均分帳單：輸入總金額與人數，輸出每人平均，固定兩位小數。`100 3` → `33.33` |
| 變體 A | 攝氏轉華氏：輸入攝氏溫度，輸出華氏，固定一位小數。銜接 Judge-073。 |
| 變體 B | 整除還是小數：輸入兩個正整數 a b，第一行輸出 `a // b`，第二行輸出 `a / b` 固定三位小數。 |
| commonMistakes | 只用 `round()` 導致 `3.5` 印成 `3.5` 而不是 `3.50`；把 `//` 用在需要小數的題目上。 |

> **待審決策**：這一關建議整關標 `allowedLanguages: ['python']`。Scratch 與 Blockly 有四捨五入積木，但沒有「不足位數補 0」的格式化能力，核心題與變體 B 的測資會過不了。比照 H01／H02 的先例處理，並同步 `README.md` 與總綱第八節。若不接受 LV.1 出現 Python 限定關，替代方案是把核心題改成保證不需補 0 的測資 —— 但那樣就教不到格式化真正的重點。

#### 第 11 關：布林值與 and / or / not

| 欄位 | 內容 |
| --- | --- |
| badge | `True` / `False` · `and` `or` `not` |
| kicker | `YES OR NO IS A VALUE` |
| goal | 把「成立或不成立」本身當成一個值，並用 and／or／not 組合多個條件。 |
| concept | `x > 5` 這種比較，算出來的結果就是 `True` 或 `False` 兩個布林值之一，可以直接印出來、也可以存進變數。`and` 要兩邊都成立；`or` 任一邊成立就好；`not` 把真假反過來。 |
| predict | 身高 118、年齡 8。`height >= 120 and age >= 6` 是 True 還是 False？把 `and` 換成 `or` 呢？ |
| 核心題 | 可以搭乘嗎：輸入身高與年齡，身高至少 120 且年齡至少 6 才能搭乘，輸出 `True` 或 `False`。**直接 print 比較式的結果，不寫 if**，示範布林值是一個值。 |
| 變體 A | 是週末嗎：輸入 1–7 的星期，輸出是否為 6 或 7。練 `or`。 |
| 變體 B | 沒有不及格：輸入分數，用 `not` 輸出「是否不及格」。 |
| commonMistakes | 寫成 `d == 6 or 7`；把 `and` 與 `or` 的意思對調；以為布林值一定要配 `if` 才能用。 |

> 放在第 10 關之後是刻意的：第 10 關的變體 B 已經用到 `and`，這一關把先前「先會用、還沒正式學」的東西補成正式概念。LV.2 的 M06 目前在 `concept` 裡重新教了一次布林值，之後可改成一句回收。

### 2.4 原第 5 關（新第 6 關）的變體 B 替換

原變體 B「分組後剩幾個」升格成新第 7 關的核心題，第 6 關需要一個新的變體 B，且**不能用到 `//` 或 `%`**（那是下一關的內容）。

建議：**總價計算** —— 輸入單價、數量、運費三個整數，輸出總金額 `單價 * 數量 + 運費`。練的是四則混合與運算優先權，與變體 A（括號）互補。

### 2.5 LV.1 連動修改清單

| 檔案 | 修改 |
| --- | --- |
| `beginner.html` | `BEGINNER_LESSONS` 重排 1–15、插入 4 關、換掉第 6 關變體 B |
| `beginner.html` | `BEGINNER_COURSE`：`progressVersion` → `'v3'`、`legacyDrafts` → `false`、新增 `skipLegacyProgress: true` |
| `beginner.html` | `migrateLegacyProgress()` 加上 `skipLegacyProgress` 保護 |
| `beginner.html` | `BEGINNER_COURSE.finishCopy` 補上型別、整除餘數、小數格式化、布林值 |
| `scripts/verify_courses.mjs` | `expectedCounts.beginner`：`11` → `15` |
| `README.md` | 第 3、5、17、291、309 行的關數與題數（31 關 93 題 → 35 關 105 題；LV.1 11 關 → 15 關；移除「含 8.5」的說法） |
| `docs/題庫總覽與學習階梯.md` | 第 6 行「本站講義合計 31 關、93 題」 |
| `docs/課程內容草稿/課程與題庫總綱.md` | 第三節「LV.1銜接節點」表格；第八節新增型別轉換與小數格式化兩列 |
| `for_AI/小學程式基礎學習地圖_第一階段.md` | LV.1 教學設計母稿：概念重編號 1–15、新增 4 張概念教學卡、依賴地圖擴充、概念 6 的變體 B 替換 |

### 2.6 LV.1 驗收

- `node scripts/verify_courses.mjs` 失敗 0，且關數檢查為 15。
- 每個新關卡的 `solution` 都能跑過自己的三組 `tests`（驗證腳本會自動執行）。
- 先備鏈檢查：第 7 關不出現 `if`；第 8 關之前不出現 `/` 求小數；第 11 關之前不出現獨立的布林變數。
- 瀏覽器實測：換到 v3 後進度從 0 開始、15 關都能切換、`?course=intermediate` 不受影響。

---

## 三、LV.2：10 關 → 14 關 ✅ 已實作（2026-09-25）

維持 `M01`–`M10` 的既有編號含義，新關卡插入後一併重排為 `M01`–`M14`，處理方式同 2.1（`pyjudge_intermediate` 的 `progressVersion` 升版）。

| 新關 | 主題 | 插入位置 | 沒有講義家的題庫 |
| --- | --- | --- | --- |
| 清單的增刪與切片 | `append` / `pop` / `insert` / `remove` / 切片 / `join` / 複製與參照 | M03 之後 | 118、120、121、125、128、132 |
| 字串組裝與格式化輸出 | 逐行累積輸出、`join`、f-string 組裝 | M02 之後 | 163、172、019 |
| 數字拆解、進位轉換與數論 | `//10` `%10` 拆位數、十↔二進位、因數、質數試除、累乘 | M04 之後 | 9、10、48、148、149、150、171、004 |
| 讀懂錯誤訊息與自訂測資 | `IndexError` / `ValueError` / `TypeError` 的讀法、最小測資、邊界測資 | M10 之後（收尾關） | 全域能力 |

另補：M01–M10 沒有任何一關處理不定行輸入／讀到 EOF，全部是「先給 N 再給 N 行」。建議併入「讀懂錯誤訊息與自訂測資」關的變體 B（對應 165）。

### 3.1 實作後的 14 關順序

| 新 | 關卡 | 來源 |
| ---: | --- | --- |
| M01 | 字串長度與索引 | 原 M01 |
| M02 | 字串逐字走訪 | 原 M02 |
| **M03** | **字串組裝與輸出格式** | ★ 新增 |
| M04 | 建立與讀取清單 | 原 M03 |
| **M05** | **清單的增刪與切片** | ★ 新增 |
| M06 | 清單統計 | 原 M04 |
| **M07** | **數字拆解與進位轉換** | ★ 新增 |
| M08 | 最大值、最小值與位置 | 原 M05 |
| M09 | 線性搜尋 | 原 M06（`concept` 改寫） |
| M10 | 清單更新與狀態模擬 | 原 M07 |
| M11 | 巢狀迴圈 | 原 M08 |
| M12 | 二維清單 | 原 M09 |
| M13 | 函式、拆解與測試 | 原 M10 |
| **M14** | **讀懂錯誤訊息與自訂測資** 🐍 | ★ 新增（收尾關） |

除 M09 的 `concept` 外，原有 10 關的教學內容**一字不改**，只改 `id` 與 `displayNumber`。

### 3.2 四個新關卡的實際內容

| 關卡 | 核心題 C | 變體 A（鞏固） | 變體 B（遷移） |
| --- | --- | --- | --- |
| M03 | 字元之間加連字號（`P-Y-T-H-O-N`） | 標題橫幅（`"=" * len(title)`） | 連續字元壓縮（`aaabb → a3 b2`） |
| M05 | 排隊叫號（`append` + `pop(0)`） | 切片後用逗號組合（`",".join(map(str, ...))`） | 複製一份不互相影響（`b = a[:]`） |
| M07 | 位數拆解（逐行輸出每一位） | 數字反轉（`rev = rev * 10 + n % 10`） | 十進位轉二進位 |
| M14 | 相鄰兩數之和：修好 `IndexError` | 一行加總：修好 `ValueError` | 讀到輸入結束（`for line in sys.stdin`） |

### 3.3 與原規格的三處調整

1. **M03 不教小數位數格式化。** 原規格寫「字串組裝與格式化輸出」。`f"{x:.2f}"` 的補 0 對齊 Scratch／Blockly 做不到，整關會被迫標成 Python 限定；而那個能力LV.1概念 8 已經教過。因此 M03 只做「片段組裝」，改名為**字串組裝與輸出格式**，三題都能三語言作答。
2. **M07 不把質數試除做成變體。** 原規格的「數論」含因數、質數試除與累乘。試除枚舉是與拆位無關的第二個新能力，放進同一關會違反「一次只增加一個主要負擔」。因此三題都是同一個 `% 10` / `// 10` 迴圈（拆位 → 反轉 → 換底），因數判斷與質數定義寫在 `concept` 裡並指向 Judge-004、048、148、171 作延伸練習；關名改為**數字拆解與進位轉換**。
3. **M14 整關標記 🐍 Python 實作。** Traceback 的內容格式與 `sys.stdin` 是 Python 專屬，Scratch／Blockly 沒有對應物。已同步 `README.md` 與總綱第八節。

原規格把 172（空心方框列印）列為字串組裝關的孤兒題，實際上 M11（原 M08）變體 B 就是空心方框，本來就有講義家，不需另外處理。

### 3.4 LV.2 連動修改清單

| 檔案 | 修改 |
| --- | --- |
| `assets/course-catalog.js` | `intermediate` 重排 M01–M14、插入 4 關、M09 `concept` 改寫 |
| `assets/course-catalog.js` | 課程設定新增 `progressVersion: 'v2'`、`legacyDrafts: false`、`skipLegacyProgress: true`，並改寫 `finishCopy` |
| `scripts/verify_courses.mjs` | `expectedCounts.intermediate`：`10` → `14` |
| `docs/課程內容草稿/LV.2講義內容母稿.md` | 改寫為 14 關（同時清掉舊檔殘留的跳脫反引號，改回正常反引號） |
| `docs/課程內容草稿/課程與題庫總綱.md` | 第一節流程、第二節總規模、第三節銜接節點、第四節階梯表、第八節支援邊界、第十一節驗收清單 |
| `docs/課程內容草稿/README.md` | LV.2 母稿的關數與主題 |
| `docs/課程內容草稿/擴充題庫規格_021-040.md` | 021–030 的「先備」欄位重新對應到新關卡 id |
| `docs/題庫總覽與學習階梯.md` | 「本站講義合計 39 關、117 題」 |
| `README.md` | 第 3、5、18、35、84、304、322 行的關數題數與 Python 限定關清單 |
| `for_AI/小學程式基礎學習地圖_第一階段.md` | 概念 11 的「未來連接」M06 → M09 |
| `測試入口.md` | 第五節連動清單補上 LV.2／LV.3 母稿與擴充題庫規格 |

`storagePrefix` 維持 `pyjudge_intermediate` 不動；與 LV.1 同樣採「不寫遷移、直接換版」，舊草稿鍵 `pyjudge_intermediate_draft_<舊 id>_<task>` 會落到新關卡上，屬已知且可接受的代價。

### 3.5 LV.2 驗收結果

- `node scripts/verify_courses.mjs` → `課程驗證通過：39 關、117 題、364 組測試。`（每題參考解答都實際用 python3 跑過全部測資）
- `node scripts/verify_editors.cjs` → `13 passed; 0 failed`
- `node --test "test/*.test.mjs" "algoplay/test/*.test.mjs"` → `48 pass / 0 fail`
- `python3 scripts/verify_problems.py` → 未動題庫，維持原狀
- 先備鏈檢查：M03 不用清單、M05 之前不出現 `append`／`pop`、M07 之前不出現 `while`（LV.1概念 13 已教）、M09 不重教布林值只回收

---

## 四、LV.3：10 關 → 16 關（本輪後續批次）

### 4.1 排序拆成兩章

現況 H01 剛講完複雜度，H02 立刻用 `sort(key=...)`，學生從沒看過排序怎麼做到、也不知道 `O(N log N)` 從哪來。拆成兩關補上：

| 新關 | 主題 | 內容 | 位置 |
| --- | --- | --- | --- |
| **基礎排序** | 氣泡、選擇、插入 | 三種都是 `O(N²)`，但交換次數與「資料已接近排好時」的表現不同。用同一份小資料畫三張追蹤表對照。核心題實作其中一種並輸出每一輪的中間狀態，讓排序過程可見。 | H01 之後 |
| **進階排序** | 希爾、合併、快速 | 希爾＝分組插入，是從 `O(N²)` 往下走的第一步；合併＝分割後合併，穩定的 `O(N log N)`；快速＝選 pivot 分割，平均 `O(N log N)` 但最壞 `O(N²)`。這一關同時交代 `O(N log N)` 的來源，以及 Python 的 `sort()` 實際在做什麼。 | 基礎排序之後、原 H02 之前 |

進階排序關的合併步驟是 LV.4「合併排序與逆序對」（211）的直接先備；快速排序的分割思路銜接 203。

### 4.2 其餘 4 關

| 新關 | 主題 | 位置 | 沒有講義家的題庫 |
| --- | --- | --- | --- |
| 雙指標與滑動視窗 | 目前只藏在 H03 的變體 B，升格為獨立關 | H03 之後 | 183、184、189、205 |
| 圖的表示法與樹 | H06／H07／H10 全部在網格上做 BFS／DFS，學生沒看過鄰接表，不知道圖可以不是格子 | H06 之前 | 198、199、200 |
| 記憶化遞迴：從遞迴到 DP | H07 教遞迴、H09 教 DP，中間缺「遞迴＋備忘錄＝DP」這座橋 | H07 之後 | 銜接全部 DP 題 |
| 二維 DP 與子序列 DP | H09 一關要撐 10 題 DP，拆出網格路徑與 LIS／LCS | H09 之後 | 193、194、195、196 |

### 4.3 重排後的 16 關

```
H01 效率與複雜度
H02 基礎排序（氣泡、選擇、插入）        ★
H03 進階排序（希爾、合併、快速）        ★
H04 排序與自訂順序（原 H02）
H05 前綴和與區間查詢（原 H03）
H06 雙指標與滑動視窗                    ★
H07 二分搜尋（原 H04）
H08 堆疊（原 H05）
H09 圖的表示法與樹                      ★
H10 佇列與廣度優先搜尋（原 H06）
H11 遞迴與深度優先搜尋（原 H07）
H12 記憶化遞迴：從遞迴到 DP             ★
H13 貪心策略（原 H08）
H14 動態規劃（原 H09）
H15 二維 DP 與子序列 DP                 ★
H16 圖搜尋綜合建模（原 H10）
```

---

## 五、LV.4：本輪不做，僅登記缺口

`README.md` 定義了 `Challenge` 級、題庫有 11 題，但沒有講義。以下 10 個概念目前是無家孤島，留待下一輪：

二分答案（204）、單調佇列／`deque`（205）、並查集（206、209）、拓撲排序與 DAG 上的 DP（207）、優先佇列 `heapq` 與 Dijkstra（208）、Kruskal 最小生成樹（209）、加權區間排程（210）、分治：合併排序與逆序對（211）、Fenwick Tree（212）、0/1 背包（213）。

LV.3 新增的進階排序關會先把合併排序的先備補上。

---

## 六、執行順序

1. ✅ 寫入本計畫
2. ✅ **LV.1 實作**：15 關重排 + 4 關新內容 + 連動修改 + `verify_courses.mjs` 通過
3. ✅ **LV.2 實作**：14 關重排 + 4 關新內容 + 連動修改 + `verify_courses.mjs` 通過（見第三節 3.1–3.5）
4. ⏸ LV.3 實作（6 關，含兩章排序）
5. ⏸ LV.4 規格與實作
